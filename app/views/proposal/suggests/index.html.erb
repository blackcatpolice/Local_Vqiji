<% content_for :title do %>合理化建议大厅<%end%>
<div class="sz_nav f12"><b>合理化建议大厅&nbsp;</b></div>
<%= render '/proposal/rc/editor'%>
<div class="list_t f16"><a class="now" href="javascript:;">大家都再说</a></div>
<div class="list">
    <% unless @suggests.empty? %>
    <%= render :collection => @suggests, :partial => 'proposal/rc/suggest' %>
    <% else %>
    <div>空的～！</div>
    <% end %>
</div>

<% unless @suggests.empty? %>
<div class="fy"><%= will_paginate @suggests %></div>
<% end %>

<script>
	$("#proposal_editor").suggestEditor();
	$("div[data-type=Suggest]").suggest();
</script>
